# IP 地理分析報表 (A系統) 技術規範說明

本文件定義「IP 地理分析報表」的產出標準與設定，未來更新或重新產出此報表時，請遵循本規範。

## 1. 基本資訊 (General Info)

| 項目 | 說明 |
|------|------|
| 報表名稱 | `A_IP_Geo_Analysis_Report.html` |
| 產生腳本 | `generate_ip_geo_report.js` |
| 資料來源 | MongoDB `QwareAi` / `Qware_A_Ticket_data_Daily` |
| GeoIP 套件 | `geoip-lite` (npm) |
| 負責人 | 陳俊良 |
| 主要目的 | 依訂單 IP 判斷台灣 vs 海外購票行為，分析地理分布、付款方式、年齡結構差異 |

---

## 2. 視覺樣式設定 (Visual Styles)

採用與 A 系統其他報表相同的 **深色主題 (Dark Theme)**。

| CSS 變數 | 值 | 說明 |
|---|---|---|
| `--bg` | `#0f172a` | 頁面背景 |
| `--card` | `#1e293b` | 卡片背景 |
| `--border` | `#334155` | 邊框色 |
| `--text` | `#e2e8f0` | 主要文字 |
| `--muted` | `#94a3b8` | 次要文字 |
| `--accent` | `#3b82f6` | 強調色（藍） |
| 台灣色 | `#4ade80` | 綠色，代表台灣 |
| 海外色 | `#60a5fa` | 藍色，代表海外 |

字體：系統預設 `-apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif`

---

## 3. IP 地理判斷邏輯 (GeoIP Logic)

1. 讀取每筆資料的 `IP` 欄位（MongoDB 欄位名稱：`IP`）
2. 使用 `geoip-lite` 套件查詢國家代碼（`country`）
3. 分類規則：
   - `country === 'TW'` → **台灣**
   - `country === 'UNKNOWN'` 或 IP 無法識別 → **無法識別**
   - 其他 → **海外**（依國家代碼分群）
4. IP 查詢結果使用記憶體 cache（`ipCache` 物件）避免重複查詢

---

## 4. 核心統計邏輯 (Core Logic)

與 A 系統營收報表一致：

- **正常交易**：`狀態 === '正常'`
- **退票交易**：`狀態 === '退票'`
- **訂單筆數**：`訂單編號` 取底線前綴後不重複計數（`Set`）
- **購票金額**：正常狀態下 `售價` 加總（支援 `Decimal128` 轉換）
- **退票手續費**：退票狀態下 `手續費` 加總

---

## 5. 關鍵組件與圖表 (Components & Charts)

### 5.1 KPI 卡片 (6 張)

| 卡片 | 內容 |
|------|------|
| 🇹🇼 台灣購票金額 | 金額 + 占比 % + 訂單筆數 + 張數 |
| 🌏 海外購票金額 | 金額 + 占比 % + 訂單筆數 + 張數 |
| ❓ IP 無法識別 | 金額 + 占比 % + 訂單筆數 |
| 🌍 海外國家數 | 不重複國家/地區數量 |
| 🇹🇼 台灣退票筆數 | 退票筆數 + 張數 + 手續費 |
| 🌏 海外退票筆數 | 退票筆數 + 張數 + 手續費 |

### 5.2 圓餅圖 (Pie Charts) × 2

使用 `Chart.js` + `chartjs-plugin-datalabels`：
- **購票金額分布**：台灣 / 海外 / 未識別（含百分比 label）
- **訂單數分布**：台灣 / 海外 / 未識別

### 5.3 橫條圖：海外前 10 國家

- 類型：`bar`（`indexAxis: 'y'`）
- 依購票金額由高至低排序
- datalabels 顯示金額數字

### 5.4 分組長條圖：年齡分布

- 台灣 vs 海外分年齡區段（`<20 / 20-29 / 30-39 / 40-49 / 50-59 / 60+`）
- 類型：`bar`（分組）

### 5.5 海外國家明細表

| 欄位 | 說明 |
|------|------|
| 國家 | 中文名稱 + 國碼 |
| 購票筆數 | 不重複訂單數 |
| 購票張數 | 實際票券數 |
| 購票金額 | NT$ 格式 |
| 退票筆數 | 不重複退票訂單數 |

顯示前 15 名，依購票金額排序。

### 5.6 付款方式對照表

台灣 vs 海外並排，欄位：付款方式 / 台灣訂單數 / 台灣金額 / 海外訂單數 / 海外金額

---

## 6. 更新方式 (Update Procedure)

```bash
# 更新報表
node generate_ip_geo_report.js

# 推送
git add A_IP_Geo_Analysis_Report.html
git commit -m "Update IP geo analysis report"
git push origin main
```

更新頻率：**手動 / 依需求**（Daily collection 資料更新後執行）

---

## 7. 國家名稱對照表 (Country Map)

| 代碼 | 中文名稱 |
|------|---------|
| TW | 台灣 |
| US | 美國 |
| JP | 日本 |
| HK | 香港 |
| SG | 新加坡 |
| KR | 韓國 |
| MO | 澳門 |
| AU | 澳洲 |
| MY | 馬來西亞 |
| TH | 泰國 |
| IN | 印度 |
| GB | 英國 |
| DE | 德國 |
| FR | 法國 |
| CA | 加拿大 |
| VN | 越南 |
| ID | 印尼 |
| PH | 菲律賓 |
| NL | 荷蘭 |
| SE | 瑞典 |
| BG | 保加利亞 |
| BD | 孟加拉 |
| NO | 挪威 |
| CN | 中國 |
| NZ | 紐西蘭 |
