# D系統 GA 每日流量報表 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Ship a standalone `D_GA_Traffic_Daily_Report.html` that shows D系統's pure Google Analytics traffic (daily PV/Click trend, activity ranking, category breakdown) with date-range filtering, wired into the daily automation and both index pages.

**Architecture:** A new generator (`generate_d_ga_traffic_daily.js`) queries the same two GA collections `generate_d_ga_funnel_report.js` already uses (`GA_D_PageViewData_Webb_202606`, `GA_D_ClickData_Webb_202606`), patches a `// ── Data Start ── / Data End ──` marker block into a static HTML template, and never touches DMP data. A shared `d_system_activity_category_map.js` module replaces the `CAT_MAP` object currently duplicated (about to be duplicated) between generators, so the manually-maintained ActivityId→category map has one source of truth.

**Tech Stack:** Node.js (`mongodb` driver, already a project dependency), Chart.js 4 + chartjs-plugin-datalabels + flatpickr (all loaded via the same CDN URLs the sibling funnel report already uses), no build step — plain static HTML/CSS/JS matching this project's existing report convention.

**Testing note:** This codebase has no unit-test framework (`package.json` has no test runner, no `*.test.js` files anywhere in the repo) — every report here is a static HTML generated by a one-off Node script and verified by (a) running the generator and checking its console output / the resulting embedded JSON, and (b) opening the file in a browser. This plan follows that existing pattern instead of inventing a test framework: verification steps use `node <script>.js` + grep-style assertions on the output file, plus a local static server + `claude-in-chrome` visual check, exactly like the tab-split work done earlier this session on `report_index.html`.

---

### Task 1: Extract shared ActivityId→category map

**Files:**
- Create: `D:\2025\AI\MongoDB\d_system_activity_category_map.js`
- Modify: `D:\2025\AI\MongoDB\generate_d_ga_funnel_report.js:19-32`

**Why first:** Both the existing funnel generator and the new generator need the exact same 43-entry `CAT_MAP`. If it's copy-pasted into the new generator, the two copies will drift the next time someone tags a new activity. Pulling it out now (before the new generator exists) means there's only ever one copy to begin with.

- [ ] **Step 1: Create the shared module**

```js
// d_system_activity_category_map.js
// Manually maintained ActivityId → category map for D系統 GA reports
// (generate_d_ga_funnel_report.js + generate_d_ga_traffic_daily.js both
// require this file — do not duplicate it, keep one source of truth).
const CAT_MAP = {
    '39590':'concert','39664':'concert','39226':'sports','39428':'sports',
    '39455':'sports', '39611':'concert','39667':'sports','39678':'kpop',
    '39496':'anime',  '39559':'concert','39649':'kpop',  '39451':'sports',
    '39584':'anime',  '39603':'kpop',   '39511':'sports','39458':'sports',
    '39682':'kpop',   '39680':'kpop',   '39494':'concert','38900':'concert',
    '39673':'kpop',   '39490':'anime',  '39453':'concert','39005':'sports',
    '39605':'kpop',   '39650':'concert','39555':'anime',  '39666':'sports',
    '39607':'kpop',   '39549':'kpop',   '39556':'anime',  '39527':'concert',
    '39696':'concert','39690':'concert','39689':'sports', '39692':'concert',
    '39665':'concert','39576':'sports', '39190':'sports', '39470':'concert',
    '39648':'sports', '39619':'concert','39524':'sports', '39526':'kpop',
    '39575':'kpop',   '39550':'kpop',   '39610':'concert',
};

module.exports = { CAT_MAP };
```

- [ ] **Step 2: Point the existing funnel generator at the shared module**

In `generate_d_ga_funnel_report.js`, find this block (currently lines 19-32):

```js
const CAT_MAP = {
    '39590':'concert','39664':'concert','39226':'sports','39428':'sports',
    '39455':'sports', '39611':'concert','39667':'sports','39678':'kpop',
    '39496':'anime',  '39559':'concert','39649':'kpop',  '39451':'sports',
    '39584':'anime',  '39603':'kpop',   '39511':'sports','39458':'sports',
    '39682':'kpop',   '39680':'kpop',   '39494':'concert','38900':'concert',
    '39673':'kpop',   '39490':'anime',  '39453':'concert','39005':'sports',
    '39605':'kpop',   '39650':'concert','39555':'anime',  '39666':'sports',
    '39607':'kpop',   '39549':'kpop',   '39556':'anime',  '39527':'concert',
    '39696':'concert','39690':'concert','39689':'sports', '39692':'concert',
    '39665':'concert','39576':'sports', '39190':'sports', '39470':'concert',
    '39648':'sports', '39619':'concert','39524':'sports', '39526':'kpop',
    '39575':'kpop',   '39550':'kpop',   '39610':'concert',
};
```

Replace it with:

```js
const { CAT_MAP } = require('./d_system_activity_category_map');
```

This is a pure data-source swap — `CAT_MAP[id]` usage elsewhere in the file (line ~125: `const cat = CAT_MAP[id] || 'concert';`) is untouched.

- [ ] **Step 3: Verify the funnel generator still runs identically**

Run: `"D:\nodejs\node.exe" generate_d_ga_funnel_report.js`
Expected: Same console output pattern as before (`Querying PageView...`, `Querying ClickData...`, `Querying daily PV/Click...`, ends by writing `D_GA_Funnel_202606_Report.html` — no new errors, no `Cannot find module` for the new require).

Then confirm the categories embedded in the regenerated file are unchanged:

Run: `node -e "const h=require('fs').readFileSync('D_GA_Funnel_202606_Report.html','utf8'); const m=h.match(/const FUNNEL_DATA = (\[[\s\S]*?\]);\n\nconst DATA_META/); const data=JSON.parse(m[1]); console.log(data.filter(d=>d.id==='39428')[0])"`
Expected: prints an object with `cat: 'sports'` (matches `CAT_MAP['39428']` above) — confirms the shared module wired through correctly.

- [ ] **Step 4: Commit**

```bash
git add d_system_activity_category_map.js generate_d_ga_funnel_report.js
git commit -m "Extract shared D-system ActivityId->category map into its own module"
```

---

### Task 2: Create the static HTML template

**Files:**
- Create: `D:\2025\AI\MongoDB\D_GA_Traffic_Daily_Report.html`

This is a plain static file (no generator run needed yet — that's Task 4). It must open correctly in a browser as-is, with empty placeholder data, so Task 2 can be verified in isolation before the generator exists.

- [ ] **Step 1: Write the full template**

```html
<!DOCTYPE html>
<html lang="zh-Hant">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width,initial-scale=1">
<title>D系統 GA 每日流量報表</title>
<!-- IP Allowlist: internal network only -->
<style>html { visibility: hidden; }</style>
<script>
  (function () {
    var ALLOWED = ['211.75.181.109','211.75.181.110','220.130.6.196','220.130.6.197','220.130.6.198','220.130.134.238','220.130.134.239','220.130.134.240','133.149.194.24'];
    function deny(ip) {
      document.documentElement.style.visibility = 'visible';
      document.body.style.cssText = 'margin:0;padding:0;background:#0a0a0a;display:flex;align-items:center;justify-content:center;min-height:100vh;';
      document.body.innerHTML = '<div style="text-align:center;font-family:Outfit,sans-serif;padding:40px"><div style="font-size:5rem;margin-bottom:20px">🔒</div><h1 style="color:#ef4444;font-size:2rem;margin:0 0 14px">存取被拒絕</h1><p style="color:#94a3b8;margin:0 0 8px">您目前的 IP 位址：<code style="background:#1e293b;padding:2px 10px;border-radius:4px;color:#f472b6">' + ip + '</code></p><p style="color:#64748b;font-size:0.88rem">此頁面僅限內部網路存取 · Access restricted to internal network only</p></div>';
    }
    var timer = setTimeout(function () { deny('timeout'); }, 6000);
    fetch('https://api.ipify.org?format=json')
      .then(function (r) { return r.json(); })
      .then(function (d) {
        clearTimeout(timer);
        if (ALLOWED.indexOf(d.ip) !== -1) { document.documentElement.style.visibility = 'visible'; }
        else { deny(d.ip); }
      })
      .catch(function () { clearTimeout(timer); deny('unknown'); });
  })();
</script>
<script src="https://cdn.jsdelivr.net/npm/chart.js@4.4.0/dist/chart.umd.min.js"></script>
<script src="https://cdn.jsdelivr.net/npm/chartjs-plugin-datalabels@2.2.0/dist/chartjs-plugin-datalabels.min.js"></script>
<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/flatpickr/dist/flatpickr.min.css">
<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/flatpickr/dist/themes/dark.css">
<script src="https://cdn.jsdelivr.net/npm/flatpickr"></script>
<style>
:root{
  --bg:#0f172a;--card:#1e293b;--border:#2d3d55;
  --text:#f1f5f9;--muted:#94a3b8;
  --blue:#3b82f6;--purple:#8b5cf6;--rose:#f43f5e;
  --green:#10b981;--amber:#f59e0b;--cyan:#06b6d4;
  --sports:#3b82f6;--concert:#8b5cf6;--kpop:#f43f5e;--anime:#10b981;
}
*{box-sizing:border-box;margin:0;padding:0}
body{background:var(--bg);color:var(--text);font-family:'Noto Sans TC',sans-serif;font-size:14px;min-height:100vh}
a{color:var(--blue);text-decoration:none}
header{background:linear-gradient(135deg,#1e3a5f,#1e293b);border-bottom:1px solid var(--border);padding:20px 28px 16px}
.hdr-row{display:flex;align-items:flex-start;gap:16px;flex-wrap:wrap}
.hdr-titles h1{font-size:20px;font-weight:700;color:var(--text)}
.hdr-tags{display:flex;gap:8px;margin-top:8px;flex-wrap:wrap}
.tag{font-size:11px;padding:3px 10px;border-radius:20px;font-weight:600}
.tag-pv{background:rgba(59,130,246,.18);color:#93c5fd;border:1px solid rgba(59,130,246,.4)}
.tag-click{background:rgba(139,92,246,.18);color:#c4b5fd;border:1px solid rgba(139,92,246,.4)}
.tag-info{background:rgba(148,163,184,.12);color:var(--muted);border:1px solid rgba(148,163,184,.25)}
.hdr-note{margin-left:auto;background:rgba(245,158,11,.08);border:1px solid rgba(245,158,11,.3);border-radius:8px;padding:10px 14px;max-width:360px;font-size:11px;color:#fcd34d;line-height:1.6}
main{padding:20px 28px}
.filter-bar{display:flex;align-items:center;gap:14px;flex-wrap:wrap;background:var(--card);border:1px solid var(--border);border-radius:10px;padding:11px 16px;margin-bottom:16px}
.filter-group{display:flex;align-items:center;gap:8px}
.filter-label{font-size:11px;color:var(--muted);white-space:nowrap}
.fp-wrap{display:flex;align-items:center;gap:5px}
.fp-input{background:#0f172a;border:1px solid var(--border);border-radius:6px;color:var(--text);font-size:12px;padding:5px 9px;width:68px;cursor:pointer;outline:none;text-align:center;transition:.15s}
.fp-input:focus{border-color:var(--blue)}
.fp-sep{color:var(--muted);font-size:12px}
.filter-actions{margin-left:auto;display:flex;gap:8px}
.filter-btn{background:var(--blue);border:none;border-radius:6px;color:#fff;font-size:12px;font-weight:600;padding:6px 14px;cursor:pointer;transition:.15s}
.filter-btn:hover{background:#2563eb}
.filter-btn-ghost{background:transparent;border:1px solid var(--border);border-radius:6px;color:var(--muted);font-size:12px;padding:6px 14px;cursor:pointer;transition:.15s}
.filter-btn-ghost:hover{border-color:var(--text);color:var(--text)}
.kpi-grid{display:grid;grid-template-columns:repeat(auto-fit,minmax(160px,1fr));gap:12px;margin-bottom:20px}
.kpi-card{background:var(--card);border:1px solid var(--border);border-radius:10px;padding:14px 16px}
.kpi-card .kpi-label{font-size:11px;color:var(--muted);margin-bottom:6px;display:flex;align-items:center;gap:5px}
.kpi-card .kpi-val{font-size:24px;font-weight:700;line-height:1.1}
.kpi-card .kpi-sub{font-size:11px;color:var(--muted);margin-top:3px}
.c-blue{color:var(--blue)}.c-purple{color:var(--purple)}.c-green{color:var(--green)}.c-rose{color:var(--rose)}.c-amber{color:var(--amber)}.c-cyan{color:var(--cyan)}
.chart-row{display:grid;grid-template-columns:1fr 1fr;gap:16px;margin-bottom:20px}
.chart-card{background:var(--card);border:1px solid var(--border);border-radius:10px;padding:16px}
.chart-card.full{grid-column:1/-1}
.chart-card h3{font-size:13px;font-weight:600;color:var(--text);margin-bottom:4px}
.chart-card .chart-sub{font-size:11px;color:var(--muted);margin-bottom:12px}
.chart-wrap{position:relative}
.table-section{background:var(--card);border:1px solid var(--border);border-radius:10px;padding:16px}
.table-section h3{font-size:14px;font-weight:600;margin-bottom:12px}
.chip-row{display:flex;gap:6px;flex-wrap:wrap;margin-bottom:14px}
.chip{background:rgba(148,163,184,.1);border:1px solid var(--border);border-radius:20px;padding:4px 12px;font-size:11px;cursor:pointer;transition:.15s}
.chip.active{background:var(--blue);border-color:var(--blue);color:#fff;font-weight:600}
.chip.sports.active{background:var(--sports)}
.chip.concert.active{background:var(--concert)}
.chip.kpop.active{background:var(--kpop)}
.chip.anime.active{background:var(--anime)}
.table-wrap{overflow-x:auto}
table{width:100%;border-collapse:collapse;font-size:12px}
th{padding:8px 10px;border-bottom:1px solid var(--border);color:var(--muted);font-weight:600;text-align:left;cursor:pointer;white-space:nowrap;user-select:none}
th:hover{color:var(--text)}
th.sort-asc::after{content:' ↑'}th.sort-desc::after{content:' ↓'}
td{padding:8px 10px;border-bottom:1px solid rgba(45,61,85,.5);vertical-align:middle}
tr:last-child td{border-bottom:none}
tr:hover td{background:rgba(59,130,246,.04)}
.badge{display:inline-block;font-size:10px;font-weight:600;padding:2px 7px;border-radius:10px}
.badge-sports{background:rgba(59,130,246,.15);color:#93c5fd}
.badge-concert{background:rgba(139,92,246,.15);color:#c4b5fd}
.badge-kpop{background:rgba(244,63,94,.15);color:#fda4af}
.badge-anime{background:rgba(16,185,129,.15);color:#6ee7b7}
.rank-up{color:var(--green);font-size:11px}
footer{margin-top:28px;padding:16px 28px;border-top:1px solid var(--border);font-size:11px;color:var(--muted);display:flex;gap:16px;flex-wrap:wrap}
footer a{color:var(--muted)}footer a:hover{color:var(--text)}
@media(max-width:760px){
  .chart-row{grid-template-columns:1fr}
  .chart-card.full{grid-column:auto}
  main{padding:14px}header{padding:14px}
  .filter-bar{gap:10px}
  .filter-actions{margin-left:0}
}
</style>
</head>
<body>
<header>
  <div class="hdr-row">
    <div class="hdr-titles">
      <h1>D系統 GA 每日流量報表</h1>
      <div class="hdr-tags">
        <span class="tag tag-pv" id="hdr-pv-dates">📄 瀏覽量資料：—</span>
        <span class="tag tag-click" id="hdr-cl-dates">🖱 點擊量資料：—</span>
        <span class="tag tag-info" id="hdr-activities">涵蓋活動：— 個</span>
        <span class="tag tag-info">更新：<span id="updateTimeLabel">—</span></span>
        <a class="tag" href="D_GA_Funnel_202606_Report.html" style="background:rgba(139,92,246,.15);color:#c4b5fd;border:1px solid rgba(139,92,246,.4);text-decoration:none;transition:.15s" onmouseover="this.style.background='rgba(139,92,246,.3)'" onmouseout="this.style.background='rgba(139,92,246,.15)'">🔗 D系統瀏覽→點擊轉換分析 →</a>
      </div>
    </div>
    <div class="hdr-note">
      📊 本報表只含 Google Analytics 流量資料（PV 瀏覽量、Click 點擊量），<strong>不含</strong>購物車/結帳等 DMP 資料——那些請看上方連結的轉換分析報表。
    </div>
  </div>
</header>

<main>

<div class="filter-bar">
  <div class="filter-group">
    <span class="filter-label">📅 日期區間</span>
    <div class="fp-wrap">
      <input id="dateFrom" class="fp-input" placeholder="06/10" readonly>
      <span class="fp-sep">–</span>
      <input id="dateTo" class="fp-input" placeholder="06/28" readonly>
    </div>
  </div>
  <div class="filter-actions">
    <button class="filter-btn-ghost" onclick="setQuickRange(1)">1天前</button>
    <button class="filter-btn-ghost" onclick="setQuickRange(7)">7天前</button>
    <button class="filter-btn-ghost" onclick="setQuickRange(30)">30天前</button>
    <button class="filter-btn" onclick="applyFilter()">套用篩選</button>
    <button class="filter-btn-ghost" onclick="resetFilter()">重置</button>
  </div>
</div>

<div class="kpi-grid">
  <div class="kpi-card">
    <div class="kpi-label">📄 總瀏覽量 (PV)</div>
    <div class="kpi-val c-blue" id="kpi-pv">—</div>
    <div class="kpi-sub" id="kpi-pv-sub">—</div>
  </div>
  <div class="kpi-card">
    <div class="kpi-label">🖱 總點擊量 (Click)</div>
    <div class="kpi-val c-purple" id="kpi-click">—</div>
    <div class="kpi-sub" id="kpi-click-sub">—</div>
  </div>
  <div class="kpi-card">
    <div class="kpi-label">📈 點擊/瀏覽比 (CTR)</div>
    <div class="kpi-val c-cyan" id="kpi-ctr">—</div>
    <div class="kpi-sub">clicks / pv</div>
  </div>
  <div class="kpi-card">
    <div class="kpi-label">🎯 涵蓋活動數</div>
    <div class="kpi-val c-green" id="kpi-activities">—</div>
    <div class="kpi-sub">目前篩選範圍內</div>
  </div>
</div>

<div class="chart-row">
  <div class="chart-card full">
    <h3>整體每日 PV / Click 趨勢</h3>
    <div class="chart-sub">D 系統全部活動加總，依日期排列</div>
    <div class="chart-wrap" style="height:280px"><canvas id="trendChart"></canvas></div>
  </div>
</div>

<div class="chart-row">
  <div class="chart-card full">
    <h3>類別佔比分布</h3>
    <div class="chart-sub">各類別 PV / Click 佔全體百分比（隨上方日期篩選連動）</div>
    <div class="chart-wrap" style="height:240px"><canvas id="catChart"></canvas></div>
  </div>
</div>

<div class="table-section">
  <h3>活動排行榜</h3>
  <div class="chip-row" id="catChips">
    <span class="chip active" data-cat="all" onclick="setCatFilter('all',this)">全部</span>
    <span class="chip sports" data-cat="sports" onclick="setCatFilter('sports',this)">運動</span>
    <span class="chip concert" data-cat="concert" onclick="setCatFilter('concert',this)">演唱會</span>
    <span class="chip kpop" data-cat="kpop" onclick="setCatFilter('kpop',this)">K-pop</span>
    <span class="chip anime" data-cat="anime" onclick="setCatFilter('anime',this)">動漫/遊戲</span>
  </div>
  <div class="table-wrap">
    <table id="rankTable">
      <thead>
        <tr>
          <th>#</th>
          <th>活動名稱</th>
          <th>類別</th>
          <th onclick="sortTable(3)" class="sort-desc">PV</th>
          <th onclick="sortTable(4)">PV 佔比</th>
          <th onclick="sortTable(5)">Click</th>
          <th onclick="sortTable(6)">Click 佔比</th>
          <th onclick="sortTable(7)">ActivityId</th>
        </tr>
      </thead>
      <tbody id="rankTableBody"></tbody>
    </table>
  </div>
</div>

</main>

<footer>
  <span>D系統 GA 每日流量報表</span>
  <span>資料來源：GA_D_PageViewData_Webb_202606 / GA_D_ClickData_Webb_202606</span>
  <a href="report_index.html">← 返回報表中心</a>
</footer>

<script>
// ── Data Start ──────────────────────────────────────────────────────────────
const DAILY_TOTAL = [];
const ACTIVITY_RANKING = [];
const PV_BY_DATE = {};
const CLICK_ACT_DAILY = {};
const DATA_META = { pvDateFrom: '', pvDateTo: '', clickDateFrom: '', clickDateTo: '', totalActivities: 0, generatedAt: '' };
// ── Data End ────────────────────────────────────────────────────────────────

Chart.register(ChartDataLabels);

const CAT_COLOR = {
  sports: { bg:'rgba(59,130,246,.75)', border:'#3b82f6', label:'運動' },
  concert:{ bg:'rgba(139,92,246,.75)', border:'#8b5cf6', label:'演唱會' },
  kpop:   { bg:'rgba(244,63,94,.75)',  border:'#f43f5e', label:'K-pop' },
  anime:  { bg:'rgba(16,185,129,.75)', border:'#10b981', label:'動漫/遊戲' },
};
const CAT_LABEL = { sports:'運動', concert:'演唱會', kpop:'K-pop', anime:'動漫/遊戲' };
const CAT_CSS   = { sports:'badge-sports', concert:'badge-concert', kpop:'badge-kpop', anime:'badge-anime' };

function fmtNum(n){ if(n==null) return '—'; return n>=10000 ? (n/10000).toFixed(1)+'萬' : n.toLocaleString(); }
function padDate(s){ const [m,d]=s.split('/'); return m.padStart(2,'0')+'/'+d.padStart(2,'0'); }

let currentCatFilter = 'all';
let sortCol = 3, sortDir = 'desc';
let liveDaily = null;    // null = use DAILY_TOTAL (unfiltered)
let liveRanking = null;  // null = use ACTIVITY_RANKING (unfiltered)
let trendChart, catChart, fpFrom, fpTo;
const allDates = DAILY_TOTAL.map(d => d.date);

document.getElementById('hdr-pv-dates').textContent = `📄 瀏覽量資料：${DATA_META.pvDateFrom} – ${DATA_META.pvDateTo}`;
document.getElementById('hdr-cl-dates').textContent = `🖱 點擊量資料：${DATA_META.clickDateFrom} – ${DATA_META.clickDateTo}`;
document.getElementById('hdr-activities').textContent = `涵蓋活動：${DATA_META.totalActivities} 個`;
document.getElementById('updateTimeLabel').textContent = DATA_META.generatedAt;

function computeRanking(from, to) {
  const rows = ACTIVITY_RANKING.map(d => {
    const r = Object.assign({}, d);
    if (PV_BY_DATE[d.id]) {
      let pv = 0;
      for (const [date, cnt] of Object.entries(PV_BY_DATE[d.id])) { if (date >= from && date <= to) pv += cnt; }
      r.pv = pv > 0 ? pv : null;
    } else { r.pv = null; }
    if (CLICK_ACT_DAILY[d.id]) {
      let cl = 0;
      for (const e of CLICK_ACT_DAILY[d.id]) { if (e.date >= from && e.date <= to) cl += e.total; }
      r.clicks = cl > 0 ? cl : null;
    } else { r.clicks = null; }
    return r;
  });
  const pvTotal = rows.reduce((s,d) => s + (d.pv||0), 0);
  const clickTotal = rows.reduce((s,d) => s + (d.clicks||0), 0);
  const pvSorted = rows.filter(d=>d.pv!=null).sort((a,b)=>b.pv-a.pv);
  pvSorted.forEach((d,i)=>{ d.pvRank=i+1; });
  const clSorted = rows.filter(d=>d.clicks!=null).sort((a,b)=>b.clicks-a.clicks);
  clSorted.forEach((d,i)=>{ d.clickRank=i+1; });
  rows.forEach(d => {
    d.pvShare = d.pv!=null ? +(d.pv/pvTotal*100).toFixed(1) : null;
    d.clickShare = d.clicks!=null ? +(d.clicks/clickTotal*100).toFixed(1) : null;
  });
  return rows;
}

function computeDaily(from, to) {
  return DAILY_TOTAL.filter(d => d.date >= from && d.date <= to);
}

function currentRanking() { return liveRanking || ACTIVITY_RANKING; }

function updateKPI(ranking, daily) {
  const pv = ranking.reduce((s,d)=>s+(d.pv||0),0);
  const click = ranking.reduce((s,d)=>s+(d.clicks||0),0);
  const activities = ranking.filter(d=>d.pv!=null||d.clicks!=null).length;
  document.getElementById('kpi-pv').textContent = fmtNum(pv);
  document.getElementById('kpi-click').textContent = fmtNum(click);
  document.getElementById('kpi-ctr').textContent = pv ? (click/pv*100).toFixed(1)+'%' : '—';
  document.getElementById('kpi-activities').textContent = activities;
  document.getElementById('kpi-pv-sub').textContent = daily.length ? `${daily[0].date} – ${daily[daily.length-1].date}` : '—';
  document.getElementById('kpi-click-sub').textContent = daily.length ? `${daily.length} 天` : '—';
}

function updateTrendChart(daily) {
  const labels = daily.map(d => d.date);
  if (trendChart) trendChart.destroy();
  trendChart = new Chart(document.getElementById('trendChart'), {
    type: 'line',
    data: {
      labels,
      datasets: [
        { label: 'PV', data: daily.map(d=>d.pv), borderColor:'#3b82f6', backgroundColor:'rgba(59,130,246,.12)', fill:true, tension:.3, yAxisID:'y' },
        { label: 'Click', data: daily.map(d=>d.clicks), borderColor:'#8b5cf6', backgroundColor:'rgba(139,92,246,.12)', fill:true, tension:.3, yAxisID:'y1' },
      ]
    },
    options: {
      responsive:true, maintainAspectRatio:false,
      plugins:{ legend:{position:'top', labels:{color:'#94a3b8'}}, datalabels:{display:false} },
      scales:{
        x:{ ticks:{color:'#94a3b8'}, grid:{color:'rgba(148,163,184,.08)'} },
        y:{ position:'left', ticks:{color:'#3b82f6'}, grid:{color:'rgba(148,163,184,.08)'}, title:{display:true,text:'PV',color:'#3b82f6'} },
        y1:{ position:'right', ticks:{color:'#8b5cf6'}, grid:{display:false}, title:{display:true,text:'Click',color:'#8b5cf6'} },
      }
    }
  });
}

function updateCatChart(ranking) {
  const cats = ['sports','concert','kpop','anime'];
  const pvTotal = ranking.reduce((s,d)=>s+(d.pv||0),0);
  const clickTotal = ranking.reduce((s,d)=>s+(d.clicks||0),0);
  const pvShare = cats.map(c => pvTotal ? +(ranking.filter(d=>d.cat===c).reduce((s,d)=>s+(d.pv||0),0)/pvTotal*100).toFixed(1) : 0);
  const clickShare = cats.map(c => clickTotal ? +(ranking.filter(d=>d.cat===c).reduce((s,d)=>s+(d.clicks||0),0)/clickTotal*100).toFixed(1) : 0);
  if (catChart) catChart.destroy();
  catChart = new Chart(document.getElementById('catChart'), {
    type: 'bar',
    data: {
      labels: cats.map(c => CAT_COLOR[c].label),
      datasets: [
        { label: 'PV 佔比 %', data: pvShare, backgroundColor:'rgba(59,130,246,.75)' },
        { label: 'Click 佔比 %', data: clickShare, backgroundColor:'rgba(139,92,246,.75)' },
      ]
    },
    options: {
      responsive:true, maintainAspectRatio:false,
      plugins:{ legend:{position:'top', labels:{color:'#94a3b8'}}, datalabels:{color:'#f1f5f9', anchor:'end', align:'top', font:{size:10}, formatter:v=>v+'%'} },
      scales:{
        x:{ ticks:{color:'#94a3b8'}, grid:{display:false} },
        y:{ ticks:{color:'#94a3b8'}, grid:{color:'rgba(148,163,184,.08)'}, beginAtZero:true },
      }
    }
  });
}

function renderTable() {
  let rows = currentRanking().filter(d => currentCatFilter==='all' || d.cat===currentCatFilter);
  const keys = ['idx','name','cat','pv','pvShare','clicks','clickShare','id'];
  const key = keys[sortCol];
  rows = rows.slice().sort((a,b) => {
    if (key==='name'||key==='cat'||key==='id') {
      const av=String(a[key]||''), bv=String(b[key]||'');
      return sortDir==='asc'? av.localeCompare(bv): bv.localeCompare(av);
    }
    const av = a[key]==null?-1:a[key], bv = b[key]==null?-1:b[key];
    return sortDir==='asc' ? av-bv : bv-av;
  });
  const tbody = document.getElementById('rankTableBody');
  tbody.innerHTML = rows.map((d,i) =>
    '<tr>' +
    '<td>'+(i+1)+'</td>' +
    '<td>'+d.name+'</td>' +
    '<td><span class="badge '+CAT_CSS[d.cat]+'">'+CAT_LABEL[d.cat]+'</span></td>' +
    '<td>'+fmtNum(d.pv)+(d.pvRank?' <span class="rank-up">#'+d.pvRank+'</span>':'')+'</td>' +
    '<td>'+(d.pvShare!=null?d.pvShare+'%':'—')+'</td>' +
    '<td>'+fmtNum(d.clicks)+(d.clickRank?' <span class="rank-up">#'+d.clickRank+'</span>':'')+'</td>' +
    '<td>'+(d.clickShare!=null?d.clickShare+'%':'—')+'</td>' +
    '<td style="color:var(--muted)">'+d.id+'</td>' +
    '</tr>'
  ).join('');
}

function sortTable(col) {
  if (sortCol === col) { sortDir = sortDir==='asc'?'desc':'asc'; }
  else { sortCol = col; sortDir = 'desc'; }
  document.querySelectorAll('#rankTable th').forEach(th=>th.classList.remove('sort-asc','sort-desc'));
  document.querySelectorAll('#rankTable th')[col].classList.add(sortDir==='asc'?'sort-asc':'sort-desc');
  renderTable();
}

function setCatFilter(cat, el) {
  currentCatFilter = cat;
  document.querySelectorAll('#catChips .chip').forEach(c=>c.classList.remove('active'));
  el.classList.add('active');
  renderTable();
}

function applyFilter() {
  const from = padDate(fpFrom.input.value);
  const to = padDate(fpTo.input.value);
  liveRanking = computeRanking(from, to);
  liveDaily = computeDaily(from, to);
  updateKPI(liveRanking, liveDaily);
  updateTrendChart(liveDaily);
  updateCatChart(liveRanking);
  renderTable();
}

function resetFilter() {
  if (allDates.length) {
    fpFrom.setDate(allDates[0], true);
    fpTo.setDate(allDates[allDates.length-1], true);
  }
  liveRanking = null;
  liveDaily = null;
  updateKPI(ACTIVITY_RANKING, DAILY_TOTAL);
  updateTrendChart(DAILY_TOTAL);
  updateCatChart(ACTIVITY_RANKING);
  renderTable();
}

function setQuickRange(days) {
  if (!allDates.length) return;
  const anchor = allDates[allDates.length-1];
  const [am,ad] = anchor.split('/').map(Number);
  const anchorDate = new Date(2026, am-1, ad);
  const desiredDate = new Date(anchorDate.getTime() - (days-1)*86400000);
  const desired = String(desiredDate.getMonth()+1).padStart(2,'0')+'/'+String(desiredDate.getDate()).padStart(2,'0');
  const from = allDates.find(d => d >= desired) || allDates[0];
  fpFrom.setDate(from, true);
  fpTo.setDate(anchor, true);
  applyFilter();
}

if (allDates.length) {
  fpFrom = flatpickr('#dateFrom', { dateFormat:'m/d', enable: allDates, defaultDate: allDates[0], disableMobile:true });
  fpTo   = flatpickr('#dateTo',   { dateFormat:'m/d', enable: allDates, defaultDate: allDates[allDates.length-1], disableMobile:true });
}

// Initial render (unfiltered)
updateKPI(ACTIVITY_RANKING, DAILY_TOTAL);
updateTrendChart(DAILY_TOTAL);
updateCatChart(ACTIVITY_RANKING);
renderTable();
</script>
</body>
</html>
```

- [ ] **Step 2: Verify the template is valid HTML and the placeholder JS doesn't throw**

Run: `node -e "const h=require('fs').readFileSync('D_GA_Traffic_Daily_Report.html','utf8'); if(!h.includes('Data Start')||!h.includes('Data End')) throw new Error('markers missing'); console.log('markers OK, length', h.length)"`
Expected: `markers OK, length <some number>` — confirms the file was written and the marker comments are intact (Task 3's generator regex depends on them being exact).

- [ ] **Step 3: Commit**

```bash
git add D_GA_Traffic_Daily_Report.html
git commit -m "Add static template for D-system GA daily traffic report"
```

---

### Task 3: Create the generator script

**Files:**
- Create: `D:\2025\AI\MongoDB\generate_d_ga_traffic_daily.js`

- [ ] **Step 1: Write the generator**

```js
require('dotenv').config({ path: __dirname + '/.env', quiet: true });
const { MongoClient, ServerApiVersion } = require('mongodb');
const fs   = require('fs');
const path = require('path');
const { CAT_MAP } = require('./d_system_activity_category_map');

const uri = process.env.MONGODB_URI_QWARE;
const client = new MongoClient(uri, {
    serverApi: { version: ServerApiVersion.v1, strict: true, deprecationErrors: true }
});

const COLL_PV    = 'GA_D_PageViewData_Webb_202606';
const COLL_CLICK = 'GA_D_ClickData_Webb_202606';
// OUT_FILE's <head> has an IP-allowlist gate (copied from D_GA_Funnel_202606_Report.html).
// This script only patches the Data markers via regex and never touches <head> — do not
// replace with a full template rewrite without carrying the gate over.
const OUT_FILE = path.join(__dirname, 'D_GA_Traffic_Daily_Report.html');

function toTWTStr(d) {
    const tw = new Date(d.getTime() + 8 * 3600000);
    const mm  = String(tw.getUTCMonth()+1).padStart(2,'0');
    const dd  = String(tw.getUTCDate()).padStart(2,'0');
    const hh  = String(tw.getUTCHours()).padStart(2,'0');
    const min = String(tw.getUTCMinutes()).padStart(2,'0');
    return `${tw.getUTCFullYear()}/${mm}/${dd} ${hh}:${min}`;
}

const twMMDD = d => {
    const t = new Date(d.getTime() + 8 * 3600000);
    return `${String(t.getUTCMonth()+1).padStart(2,'0')}/${String(t.getUTCDate()).padStart(2,'0')}`;
};

async function main() {
    try {
        await client.connect();
        const db = client.db('QwareAi');

        console.log('Querying PageView totals...');
        const pvRaw = await db.collection(COLL_PV).aggregate([
            { $group: { _id: '$ActivityId', name: { $last: '$ActivityName' }, pv: { $sum: '$EventCount' } } },
            { $sort: { pv: -1 } }
        ]).toArray();

        console.log('Querying ClickData totals...');
        const clRaw = await db.collection(COLL_CLICK).aggregate([
            { $group: { _id: '$ActivityId', name: { $last: '$GameInfoName' }, clicks: { $sum: '$EventCount' } } },
            { $sort: { clicks: -1 } }
        ]).toArray();

        console.log('Querying daily PV/Click...');
        const pvDailyRaw = await db.collection(COLL_PV).aggregate([
            { $group: { _id: { id: '$ActivityId', date: '$EventDate' }, n: { $sum: '$EventCount' } } }
        ]).toArray();
        const clDailyRaw = await db.collection(COLL_CLICK).aggregate([
            { $group: { _id: { id: '$ActivityId', date: '$EventDate' }, n: { $sum: '$EventCount' } } }
        ]).toArray();

        // Per-activity per-day maps (same shape as generate_d_ga_funnel_report.js's
        // PV_BY_DATE / CLICK_ACT_DAILY, recomputed independently here — this generator
        // never reads the funnel report's output, only the same two Mongo collections).
        const PV_BY_DATE = {};
        pvDailyRaw.forEach(r => {
            const id = r._id.id, date = twMMDD(r._id.date);
            (PV_BY_DATE[id] = PV_BY_DATE[id] || {})[date] = (PV_BY_DATE[id][date] || 0) + r.n;
        });
        for (const id of Object.keys(PV_BY_DATE)) {
            PV_BY_DATE[id] = Object.fromEntries(Object.entries(PV_BY_DATE[id]).sort((a,b) => a[0].localeCompare(b[0])));
        }

        const clDailyMap = {};
        clDailyRaw.forEach(r => {
            const id = r._id.id, date = twMMDD(r._id.date);
            (clDailyMap[id] = clDailyMap[id] || {})[date] = (clDailyMap[id][date] || 0) + r.n;
        });
        const CLICK_ACT_DAILY = {};
        for (const id of Object.keys(clDailyMap)) {
            CLICK_ACT_DAILY[id] = Object.entries(clDailyMap[id])
                .sort((a,b) => a[0].localeCompare(b[0]))
                .map(([date, total]) => ({ date, total }));
        }

        // D-system-wide daily totals: sum PV/Click across every activity for each date.
        const dailyTotalMap = {};
        pvDailyRaw.forEach(r => {
            const date = twMMDD(r._id.date);
            dailyTotalMap[date] = dailyTotalMap[date] || { date, pv: 0, clicks: 0 };
            dailyTotalMap[date].pv += r.n;
        });
        clDailyRaw.forEach(r => {
            const date = twMMDD(r._id.date);
            dailyTotalMap[date] = dailyTotalMap[date] || { date, pv: 0, clicks: 0 };
            dailyTotalMap[date].clicks += r.n;
        });
        const DAILY_TOTAL = Object.values(dailyTotalMap).sort((a,b) => a.date.localeCompare(b.date));

        // Per-activity ranking (PV/Click totals + rank + category).
        const pvMap = {};
        pvRaw.forEach((r,i) => { pvMap[r._id] = { name: r.name, pv: r.pv, pvRank: i+1 }; });
        const clMap = {};
        clRaw.forEach((r,i) => { clMap[r._id] = { name: r.name, clicks: r.clicks, clickRank: i+1 }; });
        const allIds = [...new Set([...Object.keys(pvMap), ...Object.keys(clMap)])];
        const pvTotal = pvRaw.reduce((s,r) => s + r.pv, 0);
        const clickTotal = clRaw.reduce((s,r) => s + r.clicks, 0);

        const ACTIVITY_RANKING = allIds.map(id => {
            const pv = pvMap[id], cl = clMap[id];
            return {
                id,
                name: pv?.name || cl?.name || id,
                cat: CAT_MAP[id] || 'concert',
                pv: pv?.pv ?? null,
                pvRank: pv?.pvRank ?? null,
                pvShare: pv ? +(pv.pv / pvTotal * 100).toFixed(1) : null,
                clicks: cl?.clicks ?? null,
                clickRank: cl?.clickRank ?? null,
                clickShare: cl ? +(cl.clicks / clickTotal * 100).toFixed(1) : null,
            };
        }).sort((a,b) => (b.pv||0) - (a.pv||0));

        const PV_DATES = [...new Set(pvDailyRaw.map(r => twMMDD(r._id.date)))].sort((a,b) => a.localeCompare(b));
        const CL_DATES = [...new Set(clDailyRaw.map(r => twMMDD(r._id.date)))].sort((a,b) => a.localeCompare(b));

        const DATA_META = {
            pvDateFrom: PV_DATES[0], pvDateTo: PV_DATES[PV_DATES.length-1],
            clickDateFrom: CL_DATES[0], clickDateTo: CL_DATES[CL_DATES.length-1],
            totalActivities: allIds.length,
            generatedAt: toTWTStr(new Date()),
        };

        let html = fs.readFileSync(OUT_FILE, 'utf8');

        const newData =
`// ── Data Start ──────────────────────────────────────────────────────────────
const DAILY_TOTAL = ${JSON.stringify(DAILY_TOTAL, null, 2)};
const ACTIVITY_RANKING = ${JSON.stringify(ACTIVITY_RANKING, null, 2)};
const PV_BY_DATE = ${JSON.stringify(PV_BY_DATE, null, 2)};
const CLICK_ACT_DAILY = ${JSON.stringify(CLICK_ACT_DAILY, null, 2)};
const DATA_META = ${JSON.stringify(DATA_META, null, 2)};
// ── Data End ────────────────────────────────────────────────────────────────`;

        html = html.replace(
            /\/\/ ── Data Start ──+[\s\S]*?\/\/ ── Data End ──+/,
            newData
        );

        fs.writeFileSync(OUT_FILE, html, 'utf8');
        console.log('Written to', OUT_FILE);
        console.log('DAILY_TOTAL days:', DAILY_TOTAL.length, '| Activities:', allIds.length,
                     '| PV range:', DATA_META.pvDateFrom, '-', DATA_META.pvDateTo,
                     '| Click range:', DATA_META.clickDateFrom, '-', DATA_META.clickDateTo);
    } finally {
        await client.close();
    }
}

main().catch(err => { console.error(err); process.exit(1); });
```

- [ ] **Step 2: Commit (without running yet — running against real data is Task 4)**

```bash
git add generate_d_ga_traffic_daily.js
git commit -m "Add generator for D-system GA daily traffic report"
```

---

### Task 4: Run the generator and verify the report end-to-end

**Files:** none created — this task runs Tasks 2+3's output against real data and checks it.

- [ ] **Step 1: Run the generator**

Run: `"D:\nodejs\node.exe" generate_d_ga_traffic_daily.js`
Expected: prints `Querying PageView totals...`, `Querying ClickData totals...`, `Querying daily PV/Click...`, then `Written to ...D_GA_Traffic_Daily_Report.html` followed by a summary line like `DAILY_TOTAL days: 74 | Activities: 47 | PV range: 06/10 - 08/xx | Click range: 05/27 - 08/xx` (exact numbers will differ — the assertion is that all four numbers are > 0 and the ranges are non-empty, not any specific value).

If it fails with `Cannot find module './d_system_activity_category_map'`, Task 1 wasn't completed first — stop and go back.

- [ ] **Step 2: Assert the embedded data is well-formed**

Run:
```
node -e "
const h = require('fs').readFileSync('D_GA_Traffic_Daily_Report.html','utf8');
const get = name => JSON.parse(h.match(new RegExp('const '+name+' = ([\\\\s\\\\S]*?);\\\\n(?:const|// )'))[1]);
const daily = get('DAILY_TOTAL');
const ranking = get('ACTIVITY_RANKING');
if (!daily.length) throw new Error('DAILY_TOTAL empty');
if (!ranking.length) throw new Error('ACTIVITY_RANKING empty');
if (!['sports','concert','kpop','anime'].includes(ranking[0].cat)) throw new Error('unexpected category: '+ranking[0].cat);
console.log('OK:', daily.length, 'days,', ranking.length, 'activities, top activity:', ranking[0].name, ranking[0].cat);
"
```
Expected: `OK: <N> days, <M> activities, top activity: <name> <category>` with no thrown error.

- [ ] **Step 3: Visual check in a browser**

Start a local static server and open the file (same pattern used earlier this session for `report_index.html`):

Run: `cd D:\2025\AI\MongoDB && (python -m http.server 8793 >/tmp/httpserver3.log 2>&1 &)`

Then, using the `claude-in-chrome` tools:
1. `tabs_context_mcp{createIfEmpty:true}` to get a tab
2. `navigate` to `http://localhost:8793/D_GA_Traffic_Daily_Report.html`
3. Take a screenshot and confirm: header shows non-empty PV/Click date tags and activity count, the KPI cards show non-"—" numbers, the trend chart renders a line, the category chart renders bars, and the ranking table has rows with badges
4. Click a category chip (e.g. "運動") and confirm the table filters to only that category
5. Click the "PV" column header and confirm the sort arrow toggles and row order changes
6. Click "7天前" and confirm the date inputs update and the KPI/chart numbers change
7. Close the tab, then stop the local server: `pkill -f "http.server 8793"`

Expected: no JS console errors (`read_console_messages`), all of the above interactions visibly work.

- [ ] **Step 4: Commit the generated data**

```bash
git add D_GA_Traffic_Daily_Report.html
git commit -m "Generate first D-system GA daily traffic report data"
```

---

### Task 5: Wire into daily_update.bat

**Files:**
- Modify: `D:\2025\AI\MongoDB\daily_update.bat:27-29` (add generator call), `daily_update.bat:73` (add to SYNC_FILES)

- [ ] **Step 1: Add the generator call**

Find (currently lines 27-29):
```bat
echo Running generate_d_ga_funnel_report.js...
"D:\nodejs\node.exe" generate_d_ga_funnel_report.js >> daily_log.txt 2>&1
if errorlevel 1 set FAILED_STEPS=%FAILED_STEPS%generate_d_ga_funnel_report.js;
```

Insert immediately after it (before the `generate_d_ga_funnel_cart_data.js` block):
```bat
echo Running generate_d_ga_traffic_daily.js...
"D:\nodejs\node.exe" generate_d_ga_traffic_daily.js >> daily_log.txt 2>&1
if errorlevel 1 set FAILED_STEPS=%FAILED_STEPS%generate_d_ga_traffic_daily.js;
```

- [ ] **Step 2: Add the output file to SYNC_FILES**

Find (currently line 73):
```bat
set SYNC_FILES=A_Qware_Revenue_Report_Daily.html D_GA_Funnel_202606_Report.html E_DMP_Funnel_Report.html report_index.html!EXTRA_FILES!
```

Replace with:
```bat
set SYNC_FILES=A_Qware_Revenue_Report_Daily.html D_GA_Funnel_202606_Report.html D_GA_Traffic_Daily_Report.html E_DMP_Funnel_Report.html report_index.html!EXTRA_FILES!
```

- [ ] **Step 3: Verify the bat file is still syntactically sane**

Run: `findstr /n "generate_d_ga_traffic_daily" daily_update.bat`
Expected: two lines printed — the `echo Running...` line and the `node.exe generate_d_ga_traffic_daily.js` line, confirming the insertion landed and wasn't accidentally duplicated or malformed.

Run: `findstr /n "SYNC_FILES=" daily_update.bat`
Expected: one line, containing `D_GA_Traffic_Daily_Report.html` between `D_GA_Funnel_202606_Report.html` and `E_DMP_Funnel_Report.html`.

- [ ] **Step 4: Commit**

```bash
git add daily_update.bat
git commit -m "Wire D-system GA daily traffic report into daily_update.bat"
```

---

### Task 6: Catalog entry + spec doc

**Files:**
- Modify: `D:\2025\AI\MongoDB\HTML_Report_Catalog.html` (insert new row after the existing `20-F` D_GA_Funnel row, currently ending at line 619)
- Create: `D:\2025\AI\MongoDB\REPORT_SPEC_D_GA_TRAFFIC_DAILY.md`

- [ ] **Step 1: Insert the catalog row**

Find this block (currently lines 606-619, the `20-F` row):
```html
            <tr>
                <td>20-F</td>
                <td><span class="sys-badge sys-d">D系統</span></td>
                <td class="update-time">2026/07/14 10:30</td>
                <td><a href="D_GA_Funnel_202606_Report.html" target="_blank">D_GA_Funnel_202606_Report.html</a></td>
                <td>D 系統瀏覽→點擊→購物車→結帳轉換分析（2026/06 起累計，泡泡圖+完整活動對照表，日期篩選；2026/07/14 起 A購物車/結帳每日資料隨排程自動更新，四層同步）</td>
                <td><a href="REPORT_SPEC_D_GA_FUNNEL_202606.md" class="spec-link" target="_blank">📄 規範文檔 (MD)</a></td>
                <td><span class="table-name">GA_D_PageViewData_Webb_202606 + GA_D_ClickData_Webb_202606 + trek-first-party-dmp.event</span></td>
                <td>每日 08:00（daily_update.bat）
                    <div class="run-info">
                        <span class="run-status success">✓ 成功</span>
                        <span class="run-time">2026/07/14 10:30</span>
                    </div>
                </td>
            </tr>
```

Insert immediately after it:
```html
            <tr>
                <td>20-G</td>
                <td><span class="sys-badge sys-d">D系統</span></td>
                <td class="update-time">2026/08/10</td>
                <td><a href="D_GA_Traffic_Daily_Report.html" target="_blank">D_GA_Traffic_Daily_Report.html</a></td>
                <td>D 系統純 GA 每日流量報表（整體每日 PV/Click 趨勢、活動排行榜、類別分布，日期篩選；不含 DMP 購物車/結帳資料，與 D_GA_Funnel 報表互補）</td>
                <td><a href="REPORT_SPEC_D_GA_TRAFFIC_DAILY.md" class="spec-link" target="_blank">📄 規範文檔 (MD)</a></td>
                <td><span class="table-name">GA_D_PageViewData_Webb_202606 + GA_D_ClickData_Webb_202606</span></td>
                <td>每日 08:00（daily_update.bat）
                    <div class="run-info">
                        <span class="run-status success">✓ 成功</span>
                        <span class="run-time">2026/08/10</span>
                    </div>
                </td>
            </tr>
```

- [ ] **Step 2: Write the spec doc**

```markdown
# D系統 GA 每日流量報表 技術規範說明

本文件定義「D系統 GA 每日流量報表」(`D_GA_Traffic_Daily_Report.html`) 的產出標準，未來更新或重新產出時請遵循本規範。

## 1. 基本資訊

| 項目 | 說明 |
|------|------|
| 報表名稱 | `D_GA_Traffic_Daily_Report.html` |
| 產生腳本 | `generate_d_ga_traffic_daily.js` |
| 資料來源 | MongoDB `QwareAi` / `GA_D_PageViewData_Webb_202606` + `GA_D_ClickData_Webb_202606`（純 Google Analytics 資料，**不含** DMP） |
| 負責人 | 陳俊良 |
| 主要目的 | D 系統整體每日 GA 流量趨勢、活動排行、類別分布，與 `D_GA_Funnel_202606_Report.html` 互補——那份報表混了 DMP 購物車/結帳兩層，這份報表刻意只留純 GA 資料 |

## 2. 與 D_GA_Funnel_202606_Report.html 的關係

兩份報表查詢同一組 GA collection（`GA_D_PageViewData_Webb_202606` / `GA_D_ClickData_Webb_202606`），`generate_d_ga_traffic_daily.js` 獨立執行自己的聚合查詢，**不讀取 funnel report 的輸出**，兩支腳本互不依賴，各自可以獨立重跑。

共用的部分只有 `d_system_activity_category_map.js`（43 個 ActivityId → 類別的手動對照表，2026/08/10 從 `generate_d_ga_funnel_report.js` 抽出）——兩支 generator 都 `require` 這份檔案，新增或修改活動類別只需要改這一處。

| | D_GA_Funnel_202606_Report.html | D_GA_Traffic_Daily_Report.html |
|---|---|---|
| 資料層 | PV → Click → 購物車 → 結帳（四層） | PV、Click（兩層，純 GA） |
| 視角 | 活動別轉換漏斗（泡泡圖、CTR） | D 系統整體每日趨勢 + 活動排行 + 類別分布 |
| 適合問題 | 「這個活動的瀏覽轉點擊轉購買效果如何」 | 「D 系統整體流量今天/這週怎麼樣、哪個活動/類別最熱門」 |

## 3. 資料結構（generator 注入）

```js
// DAILY_TOTAL：D系統整體每日 PV/Click（所有活動加總）
const DAILY_TOTAL = [
  { date: "06/10", pv: 12345, clicks: 3456 },
  // … 依日期升冪排序
];

// ACTIVITY_RANKING：每活動 PV/Click 總量、排名、佔比、類別
const ACTIVITY_RANKING = [
  { id: "39428", name: "...", cat: "sports", pv: 5000, pvRank: 1, pvShare: 12.3, clicks: 1200, clickRank: 3, clickShare: 8.1 },
  // … 依 pv 由大到小排序
];

// PV_BY_DATE：每活動每日 PV，供前端日期篩選時重新加總
const PV_BY_DATE = { "39428": { "06/10": 500, ... }, ... };

// CLICK_ACT_DAILY：每活動每日 Click，格式與 funnel report 一致
const CLICK_ACT_DAILY = { "39428": [{ date:"06/10", total:120 }, ...], ... };

// DATA_META：頁首標籤與統計用的中繼資料
const DATA_META = {
  pvDateFrom: "06/10", pvDateTo: "08/09",
  clickDateFrom: "05/27", clickDateTo: "08/09",
  totalActivities: 47,
  generatedAt: "2026/08/10 08:00",
};
```

### 3.1 Section Marker

```
// ── Data Start ──────────────────────────────────────────────────────────────
const DAILY_TOTAL = […];
const ACTIVITY_RANKING = […];
const PV_BY_DATE = {…};
const CLICK_ACT_DAILY = {…};
const DATA_META = {…};
// ── Data End ────────────────────────────────────────────────────────────────
```

⚠️ **勿修改 marker 字串**，否則 generator 找不到注入點。

## 4. 關鍵組件

### 4.1 日期區間篩選

單一日期區間（不像 funnel report 分 PV/Click 兩組區間），套用後同時重新計算：整體每日趨勢圖（`computeDaily()`）、活動排行榜（`computeRanking()`）、類別分布圖。錨點＝`DAILY_TOTAL` 最後一筆日期（資料端，非日曆今天），與本專案其他日期篩選報表（F黑名單、Azure每日費用、D_GA_Funnel）一致的錨點設計。

### 4.2 活動排行榜

單一表格同時顯示 PV 與 Click 兩欄（含各自排名 `pvRank`/`clickRank`、佔比 `pvShare`/`clickShare`），透過點擊欄位標題排序（`sortTable()`）達成「PV 排行榜」或「Click 排行榜」的效果，不做成分頁籤——比舊版 `D_GA_PageViewData`/`D_GA_ClickData` 報表各自獨立 TOP10 表格更精簡，避免功能重複的兩份表格。

類別篩選（`setCatFilter()`）用 chip 群組（全部/運動/演唱會/K-pop/動漫），沿用 `D_GA_Funnel_202606_Report.html` 已驗證過的類別配色與 badge 樣式（`CAT_COLOR`/`CAT_LABEL`/`CAT_CSS`）。

### 4.3 類別佔比分布

長條圖顯示各類別的 PV 佔比與 Click 佔比（兩個百分比，同一 0-100 座標軸），而非兩個量級差異可能很大的絕對值——避免 PV 動辄上萬、Click 只有數百造成圖表視覺失衡。

### 4.4 IP 白名單保護

比照 `D_GA_Funnel_202606_Report.html` 機制（`api.ipify.org` 查訪客 IP 比對 9 組授權 IP，不符即整頁換成「存取被拒絕」）。清單為手動維護的靜態內容，generator 只用 marker 區塊替換資料、不會動到 `<head>`。

## 5. MongoDB 連線資訊

| 用途 | Cluster URI | DB | Collection |
|------|-------------|-------|------------|
| 每活動 PV 總量 + 每日 PV | `MONGODB_URI_QWARE` | `QwareAi` | `GA_D_PageViewData_Webb_202606` |
| 每活動 Click 總量 + 每日 Click | `MONGODB_URI_QWARE` | `QwareAi` | `GA_D_ClickData_Webb_202606` |

## 6. 更新方式

```bash
node generate_d_ga_traffic_daily.js
git add D_GA_Traffic_Daily_Report.html
git commit -m "Update D-system GA daily traffic report"
git push origin main
```

**已排入 `daily_update.bat`**，跟在 `generate_d_ga_funnel_report.js` 之後每日 08:00 自動執行，並隨 `SYNC_FILES` 同步到 NewReport（`origin` + `company` 兩個 remote）。

## 7. 相關連結

- 目錄：`HTML_Report_Catalog.html`
- 姊妹報表：`D_GA_Funnel_202606_Report.html`（`REPORT_SPEC_D_GA_FUNNEL_202606.md`）
- 共用類別對照表：`d_system_activity_category_map.js`

---
*建立日期：2026/08/10｜使用者要求一份「純 GA、不含 DMP」的 D 系統每日流量報表，與既有 funnel report（混了四層含 DMP）區隔開來*
```

- [ ] **Step 3: Verify the catalog HTML didn't break**

Run: `node -e "require('fs').readFileSync('HTML_Report_Catalog.html','utf8').match(/20-G/) ? console.log('row found') : (()=>{throw new Error('row missing')})()"`
Expected: `row found`

- [ ] **Step 4: Commit**

```bash
git add HTML_Report_Catalog.html REPORT_SPEC_D_GA_TRAFFIC_DAILY.md
git commit -m "Add catalog entry and spec doc for D-system GA daily traffic report"
```

---

### Task 7: Add the report_index.html card

**Files:**
- Modify: `D:\2025\AI\MongoDB\report_index.html` (tab6 "流量分析" grid)

Per [[report-index-manual-cards-source-of-truth]] — a lesson learned earlier this session — this MUST be added to the MongoDB repo's copy first (it's the source `daily_update.bat` copies from every morning), not only to NewReport's copy, or the next daily sync will silently wipe it out.

- [ ] **Step 1: Add the card to tab6**

Find the D系統 PageView card inside `<div id="tab6" class="tab-content">` (added when the 流量/費用分析 tab was split earlier this session):
```html
                <div class="card">
                    <div class="card-icon icon-analysis" style="background: rgba(139, 92, 246, 0.1); color: #8b5cf6;">👁</div>
                    <div class="card-content">
                        <h3>D系統 節目介紹頁瀏覽量分析 2026/06</h3>
```

Insert a new card immediately before it (so the new daily/all-time report sits ahead of the old one-time snapshots):
```html
                <div class="card">
                    <div class="card-icon icon-analysis" style="background: rgba(59, 130, 246, 0.1); color: #3b82f6;">📊</div>
                    <div class="card-content">
                        <h3>D系統 GA 每日流量報表</h3>
                        <p>純 Google Analytics 流量（不含 DMP 購物車/結帳）：整體每日 PV/Click 趨勢、活動排行榜、類別分布，支援日期區間篩選。</p>
                    </div>
                    <div class="card-meta">
                        <span class="badge" style="background: rgba(59, 130, 246, 0.15); color: #3b82f6;">D系統 · GA</span>
                        <a href="D_GA_Traffic_Daily_Report.html" class="btn-link" style="background-color: #3b82f6;">查看報表</a>
                    </div>
                </div>
                <div class="card">
                    <div class="card-icon icon-analysis" style="background: rgba(139, 92, 246, 0.1); color: #8b5cf6;">👁</div>
                    <div class="card-content">
                        <h3>D系統 節目介紹頁瀏覽量分析 2026/06</h3>
```

- [ ] **Step 2: Verify tab6 card count**

Run: `node -e "const h=require('fs').readFileSync('report_index.html','utf8'); const t6=h.match(/<div id=\"tab6\"[\s\S]*?<div id=\"tab8\"/)[0]; console.log('tab6 cards:', (t6.match(/class=\"card\"/g)||[]).length)"`
Expected: `tab6 cards: 6` (was 5 before this task — GA traffic, GA events, PageView, ClickData, Funnel).

- [ ] **Step 3: Visual check**

Reuse the local-server + `claude-in-chrome` pattern from Task 4 / from earlier this session: serve `report_index.html` on a free port, `switchTab('tab6')` via `javascript_tool`, confirm the new card appears with a working link to `D_GA_Traffic_Daily_Report.html`, then stop the server.

- [ ] **Step 4: Commit and push**

```bash
git add report_index.html
git commit -m "Add D-system GA daily traffic report card to report_index.html"
git push origin main
```

---

### Task 8: Deploy to NewReport and sync all remotes

**Files:**
- Copy: `D:\2025\AI\MongoDB\D_GA_Traffic_Daily_Report.html` → `D:\2025\AI\NewReport\D_GA_Traffic_Daily_Report.html`
- Modify: `D:\2025\AI\NewReport\report_index.html` (same card as Task 7)

Everything up to here lives only in the `D:\2025\AI\MongoDB` repo. `daily_update.bat` will pick up `D_GA_Traffic_Daily_Report.html` automatically from tomorrow's 08:00 run (Task 5 added it to `SYNC_FILES`), but since this plan is being executed today, deploy once manually now so the report is live immediately instead of waiting until tomorrow morning.

- [ ] **Step 1: Copy the new report file into NewReport**

```bash
cp D:/2025/AI/MongoDB/D_GA_Traffic_Daily_Report.html D:/2025/AI/NewReport/D_GA_Traffic_Daily_Report.html
```

- [ ] **Step 2: Apply the identical report_index.html card edit to NewReport's copy**

Repeat Task 7 Step 1's find/insert verbatim against `D:\2025\AI\NewReport\report_index.html` (the file is otherwise kept in sync with the MongoDB copy, so the same old_string should match).

- [ ] **Step 3: Verify with the same card-count assertion as Task 7 Step 2, pointed at the NewReport copy**

Run: `node -e "const h=require('fs').readFileSync('D:/2025/AI/NewReport/report_index.html','utf8'); const t6=h.match(/<div id=\"tab6\"[\s\S]*?<div id=\"tab8\"/)[0]; console.log('tab6 cards:', (t6.match(/class=\"card\"/g)||[]).length)"`
Expected: `tab6 cards: 6`

- [ ] **Step 4: Commit in NewReport**

```bash
cd D:/2025/AI/NewReport
git add D_GA_Traffic_Daily_Report.html report_index.html
git commit -m "Add D-system GA daily traffic report and its report_index.html card"
```

- [ ] **Step 5: Push to origin, checking fast-forward safety first**

(This session learned the hard way that `git pull --rebase` across this repo's interleaved history with the `company` remote can silently drop a commit via patch-id collision — always check ancestry before choosing rebase vs merge, and verify content after, not just that the push succeeded.)

```bash
cd D:/2025/AI/NewReport
git fetch origin main
git merge-base --is-ancestor origin/main HEAD && echo "ff-safe" || echo "diverged"
```

If `ff-safe`: `git push origin main`
If `diverged`: `git pull origin main --no-rebase --no-edit` then re-run the card-count assertion (Step 3) against the merged file before pushing, to confirm the merge didn't drop the card — then `git push origin main`.

- [ ] **Step 6: Push to company, same safety check**

```bash
cd D:/2025/AI/NewReport
git fetch company main
git merge-base --is-ancestor company/main HEAD && echo "ff-safe" || echo "diverged"
```

If `ff-safe`: `git push company main`
If `diverged`: `git pull company main --no-rebase --no-edit`, re-verify (Step 3) against the merged file, then `git push origin main` (origin must carry the merge commit too — both remotes must end up pointing at the same commit) followed by `git push company main`.

- [ ] **Step 7: Final verification against what's actually on both remotes**

```bash
cd D:/2025/AI/NewReport
git show origin/main:report_index.html | grep -c "D系統 GA 每日流量報表"
git show company/main:report_index.html | grep -c "D系統 GA 每日流量報表"
git show origin/main:D_GA_Traffic_Daily_Report.html | grep -c "Data Start"
```
Expected: `1` for each of the three commands — confirms the card text and the new report file are actually present in the pushed history on both remotes, not just that a push command returned success.

---

## Summary of files touched

| File | Task | Change |
|---|---|---|
| `d_system_activity_category_map.js` | 1 | New shared module |
| `generate_d_ga_funnel_report.js` | 1 | Require shared CAT_MAP instead of inline object |
| `D_GA_Traffic_Daily_Report.html` | 2, 3, 4, 8 | New report page |
| `generate_d_ga_traffic_daily.js` | 3 | New generator |
| `daily_update.bat` | 5 | New generator call + SYNC_FILES entry |
| `HTML_Report_Catalog.html` | 6 | New catalog row |
| `REPORT_SPEC_D_GA_TRAFFIC_DAILY.md` | 6 | New spec doc |
| `report_index.html` (MongoDB + NewReport) | 7, 8 | New card in tab6 |
