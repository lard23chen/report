> ⚠️ **存取限制** — 本規範對應的報表已設有 IP 白名單，僅限授權網路存取。

# Railway 專案展示系統 (Railway Project Demo)
## 視覺規範 (Visual Specifications)

### 1. 核心風格 (Theme)
*   **設計語彙**：採用 **Glassmorphism (玻璃擬態)** 效果，強調現代感與透明度。
*   **主色調**：`#42f5b6` (Railway Mint Green) 與深色底色 (`#0a0a0a`) 的對比。
*   **排版**：流動式佈局 (Fluid Layout)，支援各種螢幕尺寸。

### 2. UI 組件 (UI Components)
*   **Hero Section**：動態漸層背景，展示專案核心價值。
*   **功能展示卡片**：帶有懸浮放大效果 (Scale on Hover) 與柔和陰影。
*   **即時狀態燈**：顯示 Railway Server 的存活狀態。

---

## 功能組件 (Functional Components)

### 1. 互動演示 (Live Interaction)
*   **動態元件**：前端使用原生 JavaScript 或 React 實作即時 UI 饋送。
*   **API 串接**：展示從 Railway 後端獲取的模擬數據。

### 2. 技術架構 (Technical Stack)
*   **託管平台**：**Railway.app** (Production Environment)。
*   **後端系統**：Node.js / Express。
*   **資料來源**：`Railway_Prod_DB`。

---

## 操作流程 (Operating Procedures)

### 1. 存取與啟動 (Access & Initialization)
*   **進入頁面**：點擊專屬 URL 進入展示頁面。
*   **自動健康檢查**：系統啟動時會自動呼叫後端 `/health` API，若狀態燈變為**綠色**表示生產環境運行正常。

### 2. 功能操作 (Interaction)
*   **即時數據檢視**：頁面載入後，系統會從 Railway 數據庫抓取最新的 Demo 資料並動態渲染至卡片中。
*   **組件切換**：點擊分頁或按鈕可切換不同的 UI 展示模組，所有切換過程皆有平滑過場動畫。

### 3. 反饋回報 (Feedback)
*   若遇到 API 逾時，頁面會自動顯示「連線重試中」提示，使用者無需手動重新整理。

---

## 測試案例 (Test Cases)

> 測試環境：`https://project1-production-76e0.up.railway.app/demo.html`
> 更新日期：2026/04/08

### TC-DEMO-001：頁面正常載入與健康狀態燈

| 欄位 | 內容 |
|------|------|
| **前置條件** | Railway 伺服器正常運行 |
| **測試步驟** | 1. 瀏覽器開啟 `demo.html` URL<br>2. 等待頁面完整載入 |
| **預期結果** | 頁面載入完成，右上角狀態燈顯示**綠色**，表示 `/health` API 回應正常 |
| **異常情境** | 伺服器停機時狀態燈顯示紅色，並出現「連線重試中」提示 |

### TC-DEMO-002：Chat Widget 開啟與關閉

| 欄位 | 內容 |
|------|------|
| **前置條件** | 頁面已正常載入 |
| **測試步驟** | 1. 點擊右下角聊天氣泡圖示<br>2. 確認對話視窗展開<br>3. 再次點擊或按關閉按鈕 |
| **預期結果** | 對話視窗正確展開與收合，動畫流暢無閃爍 |

### TC-DEMO-003：點擊建議問題提問

| 欄位 | 內容 |
|------|------|
| **前置條件** | Chat Widget 已開啟 |
| **測試步驟** | 分別點擊以下建議問題：<br>1. 「如何申請退款？」<br>2. 「送貨需要幾天？」<br>3. 「支援哪些付款方式？」<br>4. 「忘記密碼怎麼辦？」 |
| **預期結果** | 每個建議問題點擊後自動填入輸入框並送出，AI 在 3 秒內回覆相關答案 |

### TC-DEMO-004：自由輸入問題

| 欄位 | 內容 |
|------|------|
| **前置條件** | Chat Widget 已開啟 |
| **測試步驟** | 1. 在輸入框輸入「OP點數可以折抵購票嗎？」<br>2. 按下送出或 Enter |
| **預期結果** | AI 回覆包含 OP 點數折抵相關說明，`needsHuman` 為 `false` |

### TC-DEMO-005：超出知識庫範圍的問題

| 欄位 | 內容 |
|------|------|
| **前置條件** | Chat Widget 已開啟 |
| **測試步驟** | 1. 輸入「今天天氣如何？」<br>2. 按下送出 |
| **預期結果** | AI 回覆「很抱歉，這個問題需要由客服人員為您處理。」，`needsHuman` 為 `true` |

### TC-DEMO-006：API 逾時重試提示

| 欄位 | 內容 |
|------|------|
| **前置條件** | 模擬網路斷線或 Railway 服務暫停 |
| **測試步驟** | 1. 開啟 demo.html<br>2. 使網路連線中斷 |
| **預期結果** | 頁面顯示「連線重試中」提示，狀態燈轉為紅色；恢復連線後自動重連，無需手動刷新 |

### TC-DEMO-007：功能卡片懸浮效果

| 欄位 | 內容 |
|------|------|
| **前置條件** | 頁面已正常載入（桌面版瀏覽器） |
| **測試步驟** | 1. 移動滑鼠至任一功能展示卡片上方 |
| **預期結果** | 卡片出現 Scale on Hover 放大動畫與柔和陰影，離開後恢復原狀 |

### TC-DEMO-008：RWD 響應式佈局驗證

| 欄位 | 內容 |
|------|------|
| **前置條件** | 使用 Chrome DevTools 模擬裝置 |
| **測試步驟** | 分別切換至以下解析度：Mobile (375px)、Tablet (768px)、Desktop (1440px) |
| **預期結果** | 各解析度下版面均正常顯示，無元素重疊或溢出，Chat Widget 位置固定於右下角 |
