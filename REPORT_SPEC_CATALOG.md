# HTML_Report_Catalog.html 報表總目錄 維護規範

本文件定義「Qware 報表總目錄」(`HTML_Report_Catalog.html`) 的結構與維護規則。

## 1. 基本資訊

| 項目 | 說明 |
|------|------|
| 檔名 | `HTML_Report_Catalog.html` |
| 用途 | 所有報表的總目錄，含報表連結、規範文檔連結、資料來源與排程資訊 |
| 維護方式 | **純手動維護**（無 generator，不會被腳本覆蓋） |
| 部署 | GitHub Pages (`https://lard23chen.github.io/report/HTML_Report_Catalog.html`) |

> ⚠️ `update_catalog.js`、`append_catalog.js`、`update_hrefs.js` 等是早期一次性腳本（欄位為舊版 7 欄、中文已亂碼），**勿再使用**。

## 2. 表格結構

8 欄：編號 / 系統 / 更新時間 / 檔名(超連結) / 內容說明 / 視覺規範與功能組件 / 資料表(Sources) / 排程。

區段（section header 列，`colspan="8"` 帶底色）依序為（2026/07/20 盤點）：

1. 📍 主控台與概覽儀表板
2. 📊 A 系統 營業分析報告
3. ✨ 演唱會與專案深度評估
4. 🌐 流量分析與 DMP 行銷
5. 🛒 E 系統與其他分析
6. 🚀 ibon 票券客服 QA
7. ⏰ 自動排程程式
8. 🤖 AI 工具
9. 🔍 建檔檢核使用

> **2026/07/20**：「🏠 ALEX 生活管理」區段（原第 6 項，編號 26–30·36–37·48）已整段移除，個人生活/資產類報表不再出現於本目錄，僅保留獨立子目錄 `ALEX_Life_Catalog.html`（不對外連結，需知道網址才能開啟）。

## 3. 維護規則

### 3.1 編號（流水 ID）

- **編號是流水 ID，不是排序位置**：新列一律取「目前最大編號 + 1」，**不重排既有編號**。
- 歷史上已有缺號（15、26–29）與重複（23 兩列），屬既成事實，不需修復。

### 3.2 報表 `<title>` 需標註目錄編號

- 每支報表 HTML 的 `<title>` 開頭一律加 `[編號] ` 前綴，與本目錄「編號」欄一致，方便從瀏覽器分頁 / 書籤反查目錄列（例：`<title>[20] GA 事件流量深度分析</title>`）。
- 新增報表、或既有報表確認編號時，記得同步改 `<title>`；若該報表由 generator 腳本**完整覆寫整個 HTML**（腳本內含 `<title>...</title>` 字面樣板），前綴要同步寫進腳本，否則下次自動產出會被洗掉（見 CLAUDE.md「HTML 與 generator 腳本必須同步」）。
- 若 generator 腳本是「讀回既有 HTML、只 patch 資料區塊」（`fs.readFileSync(OUT_FILE)` 後字串替換）的寫法，`<title>` 不會被覆寫，不需要改腳本。
- 封存庫（`report-archive-*`）與外部連結（Streamlit App 等）不算「本機可編輯的程式」，不強制加編號前綴。
- 2026/08/12 盤點時發現編號重複（23 三次、43/44 各兩次，見 §3.1）——重複的兩支報表各自標註各自的編號即可，不必勉強統一。

### 3.3 列的擺放位置

- 新列放進**對應系統區段**內；月報列依**年月由新到舊**排列，最新月份放區段最上方。
- Tab 篩選由 JS 在載入時依 section header 自動指派 `data-group`，**新列不需要加任何 class 或屬性**，放對區段即可。

### 3.4 更新時間欄

- 填報表檔案的**實際產出時間**，格式 `YYYY/MM/DD HH:MM`。
- 可用 `git log -1 --format="%ad" --date=format:"%Y/%m/%d %H:%M" -- <檔案>` 查詢。

### 3.5 新增月報的時機

依 CLAUDE.md 規範：**每月新報表產出時，同步在目錄加入對應列**。A 系統月報（`generate_monthly_report.js` 每月 2 日自動產出）與 E 系統月報（依 `REPORT_SPEC_E_INDEX_DASHBOARD.md` SOP 手動產出）都適用；目錄列目前不會自動新增，需手動補。

### 3.6 封存報表的連結

- 已結案報表搬到封存庫後（規則見 `REPORT_SPEC_ARCHIVE.md`），目錄**保留原列**，僅將檔名超連結改指 `https://lard23chen.github.io/report-archive-YYYY/<檔名>`。
- 主控台區段有封存庫入口列（編號 43、44）。

### 3.7 私有報表列（不上 GitHub Pages 的報表）—— 2026/07/20 起停用

> 本節規則因「ALEX 生活管理」區段整段移除（見 §2 註記、§4 變更紀錄）而不再適用，保留僅供查閱歷史脈絡。個人類報表現況請查 `ALEX_Life_Catalog.html`，不透過主目錄索引。

- 內容敏感（如個人財務）的報表**不進公開 repo、不上 Pages**，HTML 列入 `.gitignore`，改發佈到預設私有的 Claude Artifact（需登入本人 claude.ai 帳號才能開啟）。
- ~~主目錄仍保留一列供索引：檔名欄不放 Artifact 連結，標示「🔒 私有」並指引到 `ALEX_Life_Catalog.html`~~（2026/07/20 起該列已移除）；Artifact 連結放在 `ALEX_Life_Catalog.html` 對應列（連結本身非機密，存取由 claude.ai 登入控制）。
- 首例：編號 48「個人資產配置分析」，詳見 `REPORT_SPEC_ALEX_ASSET.md`。

## 4. 變更紀錄

- **2026/08/12**：新增 §3.2「報表 `<title>` 需標註目錄編號」規則，並將現有 33 支本機可編輯報表的 `<title>` 補上 `[編號] ` 前綴（含對應仍會完整覆寫 HTML 的 generator 腳本同步修改，讀回既有 HTML 只 patch 資料的腳本則不需要動）；原 §3.2–3.6 依序遞補為 §3.3–3.7。
- **2026/07/20**：移除「🏠 ALEX 生活管理」整個區段（編號 26–30·36–37·48 兩列），主目錄不再索引個人生活/資產類報表；區段清單重新盤點（10 → 9 個區段）；§3.7（原 §3.6）私有報表列規則因而停用。
- **2026/07/17**：新增編號 48「Alex 個人資產配置分析」於 ALEX 生活管理區；同日改為私有發佈（報表 HTML 退出公開 repo 與 Pages、git 歷史重寫移除，Artifact 連結列於 `ALEX_Life_Catalog.html`）。新增 3.6 私有報表列規則；區段清單重新盤點（5 → 10 個區段）。
- **2026/07/08**：歷史報表封存 — 19 檔搬至 `report-archive-2025` / `report-archive-2026`，對應列連結改指封存庫；主控台區段新增封存庫入口列（編號 43、44）。詳見 `REPORT_SPEC_ARCHIVE.md`。
- **2026/07/06**：補齊落後的月報列 — A系統 2026年04/05/06月（編號 36–38）、E系統 2026年03/05/06月（編號 39–41）；主控台兩列更新時間同步至當日。建立本規範文檔。

---
*Last Updated: 2026/08/12*
