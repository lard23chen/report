# A 系統每日營業快訊報表 技術規範說明

本文件定義「A 系統每日營業快訊分析報表」的產出標準與設定。

## 1. 基本資訊

| 項目 | 說明 |
|------|------|
| 報表名稱 | `A_Qware_Revenue_Report_Daily.html` |
| 產生腳本 | `generate_a_daily_report.js` |
| 資料來源 | MongoDB `QwareAi` / `Qware_A_Ticket_data_Daily` |
| 負責人 | 陳俊良 |
| 主要目的 | 彙整 A 系統 Daily 集合中的全部交易，提供每日即時營業數字與活動排行 |

---

## 2. 視覺樣式設定

採用**淺色主題 (Light / Cyan Theme)**，與月度報表區隔。

| CSS 變數 | 值 | 說明 |
|---|---|---|
| `--bg-color` | `#e0f7fa` | 頁面背景（淺青） |
| `--card-bg` | `#ffffff` | 卡片背景 |
| `--text-primary` | `#006064` | 主要文字 |
| `--text-secondary` | `#00838f` | 次要文字 |
| `--accent-color` | `#00acc1` | 強調色（青藍） |

字體：`Inter`（Google Fonts）

---

## 3. 核心統計邏輯

- **正常交易**：`狀態 === '正常'`
- **退票交易**：`狀態 === '已退票'` 或 `'退票'`
- **訂單筆數**：`訂單編號` 取底線前綴後去重（`Set`）
- **購票金額**：正常狀態 `售價` 加總（支援 `Decimal128` → `parseFloat`）
- **退票手續費**：退票狀態 `手續費` 加總
- **客單價 (AOV)**：`總金額 / 總訂單數`

---

## 4. 關鍵組件

### 4.1 KPI 卡片（6 張）

| 卡片 | 說明 |
|------|------|
| 總購票金額 | NT$ 格式，含小計文字 |
| 訂單筆數 | 不重複訂單數 |
| 購票張數 | 正常狀態 row 數 |
| 客單價 | 金額 / 訂單數 |
| 退票張數 | 退票 row 數 |
| 退票手續費 | 手續費合計 |

### 4.2 每日趨勢折線圖

- 使用 Chart.js，X 軸為日期（升序），Y 軸為當日購票金額
- 每個日期點顯示當日銷售前一名活動名稱

### 4.3 退票趨勢折線圖

- X 軸為退票日期，Y 軸為當日手續費合計
- 退票日期優先取 `退票時間`，無值則取 `交易時間`

### 4.4 每日快報表格

| 欄位 | 說明 |
|------|------|
| 日期 | YYYY-MM-DD |
| 訂單數 | 不重複 |
| 張數 | 正常 row 數 |
| 金額 | NT$ |
| 退票張數 | |
| 退票手續費 | |

時間倒序排列（最新在前）。

### 4.5 前 5 名活動（by 金額 & by 張數）

- 各欄位：活動名稱 / 訂單數 / 張數 / 金額 / 占比 %
- 金額排行附場次子表（依演出時間升序）

### 4.6 退票前 5 名活動

| 欄位 | 說明 |
|------|------|
| 活動名稱 | |
| 退票訂單數 | |
| 退票張數 | |
| 手續費合計 | |

### 4.7 付款方式分析

欄位：付款方式 / 訂單數 / 張數 / 金額 / 占比 %

### 4.8 銷售點分析

欄位：銷售點 / 訂單數 / 張數 / 金額 / 占比 %

---

## 5. 更新方式

```bash
node generate_a_daily_report.js
git add A_Qware_Revenue_Report_Daily.html
git commit -m "Update A daily revenue report"
git push origin main
```

更新頻率：**每日 08:00**（自動排程）
