# A 系統每日營業快訊報表 技術規範說明

> ⚠️ **存取限制** — 本規範對應的報表已設有 IP 白名單，僅限授權網路存取。


本文件定義「A 系統每日營業快訊分析報表」的產出標準與設定。

## 1. 基本資訊

| 項目 | 說明 |
|------|------|
| 報表名稱 | `A_Qware_Revenue_Report_Daily.html` |
| 產生腳本 | `generate_a_daily_report.js` |
| 資料來源 | MongoDB `QwareAi` / `Qware_A_Ticket_data_Daily` |
| 負責人 | 陳俊良 |
| 主要目的 | 彙整 A 系統 Daily 集合中的全部交易，提供每日即時營業數字與活動排行 |

---

## 2. 視覺樣式設定

採用**淺色主題 (Light / Cyan Theme)**，與月度報表區隔。

| CSS 變數 | 值 | 說明 |
|---|---|---|
| `--bg-color` | `#e0f7fa` | 頁面背景（淺青） |
| `--card-bg` | `#ffffff` | 卡片背景 |
| `--text-primary` | `#006064` | 主要文字 |
| `--text-secondary` | `#00838f` | 次要文字 |
| `--accent-color` | `#00acc1` | 強調色（青藍） |

字體：`Inter`（Google Fonts）

---

## 3. 核心統計邏輯

- **排除 B 開頭訂單（2026/07/09 起）**：MongoDB 查詢以 `"訂單編號": { $not: /^B/ }` 排除訂單編號 B 開頭的訂單，全報表統計皆不含 B 訂單；頁首 Data Source 列標明。與週報、月報、首頁月份統計口徑一致。
- **正常交易**：`狀態 === '正常'`
- **退票交易**：`狀態 === '已退票'` 或 `'退票'`
- **訂單筆數**：`訂單編號` 取底線前綴後去重（`Set`）
- **購票金額**：正常狀態 `售價` 加總（支援 `Decimal128` → `parseFloat`）
- **退票手續費**：退票狀態 `手續費` 加總
- **客單價 (AOV)**：`總金額 / 總訂單數`

---

## 4. 關鍵組件

### 4.1 KPI 卡片（6 張）

| 卡片 | 說明 |
|------|------|
| 總購票金額 | NT$ 格式，含小計文字 |
| 訂單筆數 | 不重複訂單數 |
| 購票張數 | 正常狀態 row 數 |
| 客單價 | 金額 / 訂單數 |
| 退票張數 | 退票 row 數 |
| 退票手續費 | 手續費合計 |

### 4.2 每日趨勢折線圖

- 使用 Chart.js，X 軸為日期（升序），Y 軸為當日購票金額
- 每個日期點顯示當日銷售前一名活動名稱

### 4.3 退票趨勢折線圖

- X 軸為退票日期，Y 軸為當日手續費合計
- 退票日期優先取 `退票時間`，無值則取 `交易時間`

### 4.4 每日快報表格（每日交易統計）

| 欄位 | 說明 |
|------|------|
| 日期 | YYYY-MM-DD |
| 訂單數 | 不重複 |
| 張數 | 正常 row 數 |
| 電子票 | `取票方式 === '未列印'`，張數 + 括號小字占當日張數比例（2026/07/13 新增） |
| 紙票 | `取票方式 === '已取'`，同上格式（2026/07/13 新增） |
| 金額 | NT$ |
| 退票張數 | |
| 退票手續費 | |

時間倒序排列（最新在前）。總計列同樣顯示電子票/紙票總張數與整體占比（`ttc()` helper 產生儲存格）。

### 4.5 前 5 名活動（by 金額 & by 張數）

- 各欄位：活動名稱 / 訂單數 / 張數 / **電子票 / 紙票** / 金額 / 占比 %
- **電子票/紙票欄**（2026/07/13 新增）：顯示張數 + 括號小字占**該節目張數**比例；判定規則與週報/月報/report_index 一致（`取票方式` 未列印=電子票、已取=紙票）。實作：`eventStats` 累計 `eTickets`/`paperTickets`，`renderRankTable()` 的欄位陣列加入 `'eTickets','paperTickets'` 型別分支；退票排行不套用
- 金額排行附場次子表（依演出時間升序）

### 4.6 退票前 5 名活動

| 欄位 | 說明 |
|------|------|
| 活動名稱 | |
| 退票訂單數 | |
| 退票張數 | |
| 手續費合計 | |

### 4.7 付款方式分析

欄位：付款方式 / 訂單數 / 張數 / 金額 / 占比 %

### 4.8 銷售點分析

欄位：銷售點 / 訂單數 / 張數 / 金額 / 占比 %

---

## 5. 更新方式

```bash
node generate_a_daily_report.js
git add A_Qware_Revenue_Report_Daily.html
git commit -m "Update A daily revenue report"
git push origin main
```

更新頻率：**每日 08:00**（自動排程）

---

## 6. 月度分析報表（A_Qware_Revenue_Report_YYYY年MM月_分析報表）

月度報表與每日報表為不同系列。**2026/07 起統一由通用腳本 `generate_monthly_report.js` 產出**（每月 2 日排程自動執行，可帶參數手動指定月份；UI 模板抽取自 `generate_report_feb_2026.js`），並自 2026/07/09 起排除訂單編號 B 開頭之訂單。詳見 `REPORT_SPEC_REVENUE_A.md` §8。

### 6.1 月度報表特有元件

月度報表在每日營收趨勢圖、退票趨勢圖之後，**額外新增「每日雲端費用趨勢圖」**：

| 項目 | 規格 |
|------|------|
| 資料來源 | `AzureMonthlyCost_Daily`，篩選 `Date` regex `^YYYY/MM` |
| 費用計算 | `sysA + sysD + shared + member`（**排除 E系統**，不使用 `TotalRevenue`） |
| 圖表類型 | Line chart（填色），天空藍 `#38bdf8` |
| 節點標記 | `chartjs-plugin-datalabels`，每點顯示 `$XX.XX`，0 值不顯示 |
| 位置 | **每日營收趨勢圖（trendChart）之後、退票趨勢圖之前**（透過 uiTemplate 字串替換插入） |

### 6.2 summaryData 新增欄位

```js
costTrendDates  // string[]  日期陣列（YYYY/MM/DD）
costTrendValues // number[]  對應每日費用（排除E系統，已四捨五入至小數點後2位）
```

### 6.3 注意事項

- 月度報表 UI template 從 `generate_report_feb_2026.js` 動態抽取，修改 HTML 元件時需同步更新該檔案
- `ChartDataLabels` 已在 template 中透過 `<script>` 引入，費用圖可直接使用 `plugins: [ChartDataLabels]`

---
*最後更新：2026/07/13（銷售排行兩表與每日交易統計表均新增電子票/紙票張數與占比欄，詳見 §4.4 / §4.5）*
