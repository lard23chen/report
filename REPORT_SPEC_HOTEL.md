# 國旅住宿記錄分析 技術規範說明

本文件定義「國旅住宿記錄分析」（`hotel.html`）的來源與產出方式。

## 1. 基本資訊

| 項目 | 說明 |
|------|------|
| 報表名稱 | `hotel.html` |
| 內容來源 | Google Sheets 住宿記錄試算表（4 月–12 月，26 筆入住紀錄） |
| 產生方式 | 手動轉製：匯出試算表 CSV，解析後彙總為固定的 JS 資料常數嵌入 HTML（無 generator 腳本，一次性快照） |
| 資料來源 | 無資料庫，來自使用者提供的 Google Sheets 連結 |
| 主要目的 | 彙總個人國旅住宿花費、晚數、飯店分布與已知回饋/補助 |

## 2. 核心邏輯

- 一筆「入住紀錄」需同時有月份、飯店名稱、與數字型「天數」欄位；只有月份+名稱但沒有天數的列（如餐廳消費、住宿券）歸類為「其他消費」，不計入住宿統計。
- 價格 `$0` 代表以點數兌換入住（哩程/會員點數），KPI 卡片另計為「點數兌換入住」次數。
- 平均每晚房價 = 總花費 ÷ 總晚數，與試算表原始備註列的 `$3,094` 數字互相驗證一致。
- 「已知回饋/補助」區塊的金額（公司補助、三倍券、安心旅遊等）逐字取自試算表統計列的備註文字，僅供參考加總，並未逐筆核銷。

## 3. 更新方式

若來源試算表有新增紀錄：
1. 重新匯出 CSV（`https://docs.google.com/spreadsheets/d/1LJMf99ongYEyhs6FnFGtOTAOjigAVL3YRiHoOxHhaIU/export?format=csv&gid=0`）
2. 依「天數欄位是否為數字」重新切分入住紀錄 / 其他消費
3. 手動更新 `hotel.html` 內嵌的 `monthMap`／`stays`／`extras` 三個 JS 常數與 KPI 卡片數字
4. 同步更新 `ALEX_Life_Catalog.html` 對應列的更新時間

## 4. 相關連結

- 目錄：`ALEX_Life_Catalog.html`（需密碼）
